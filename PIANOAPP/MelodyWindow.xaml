<Window x:Class="PIANOAPP.MelodyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PIANOAPP"
        mc:Ignorable="d"
        Title="MelodyWindow" Height="500" Width="400">
    <Grid>
        
        <!-- Panel dla listy utworów -->
        <StackPanel Name="RecordedSongsPanel" Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="White" Margin="0,0,10,0"  Grid.Column="0">
            <!-- Nagłówek -->
            <TextBlock Text="Dostępne utwory:" FontWeight="Bold" FontSize="18" Margin="0,0,0,10" Foreground="#FF722840"/>

            <!-- Lista utworów -->
            <ListBox Name="SongListBox" SelectionChanged="OnSongSelected"  BorderThickness="2" Background="#FFA79292" Padding="5" BorderBrush="#FF3D3535">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="16" Foreground="Black" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

    </Grid>
</Window>
